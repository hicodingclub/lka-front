This is the folder to store user uploaded files, pictures, etc. We need to link to the "public" (or "public-admin" directory after 'ng build'.

Go to 'front-end', or 'front-end-admin', run this command:

npm run build

- it will do build and do 'storage' folder link for you.
